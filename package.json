{
  "name": "entropretty",
  "type": "module",
  "scripts": {
    "build": "npm run bundle",
    "bundle": "rm -rf ./out && pnpm rollup -c rollup.config.js",
    "start": "npx serve ."
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.18.1"
  }
}
